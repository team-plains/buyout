spring.datasource.url=jdbc:postgresql://localhost:5432/buyout
spring.datasource.username=crobles22
spring.datasource.password=password



#create is destructive- if it detects a change on restart, it will drop and reload recreate the tables
spring.jpa.hibernate.ddl-auto=create
#spring.jpa.hibernate.ddl-auto=update
#only reload on changes to the columns

spring.mvc.hiddenmethod.filter.enabled=true